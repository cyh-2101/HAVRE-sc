{
    admin off
    email {$HAVRE_TLS_EMAIL}
}

{$HAVRE_PUBLIC_HOST} {
    encode zstd gzip
    header {
        Strict-Transport-Security "max-age=31536000; includeSubDomains"
        X-Content-Type-Options "nosniff"
        Referrer-Policy "no-referrer"
        -Server
    }
    reverse_proxy api:8000 {
        health_uri /health/live
        health_interval 10s
        health_timeout 3s
    }
}
